[build]
  command = "npm install && npm run build"
  publish = "build"

# Disable CI mode so warnings do NOT break the build
[build.environment]
  CI = "false"

# React SPA redirect
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
